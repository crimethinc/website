<%= render_themed 'articles/list',
                  articles:      @articles,
                  title:         tt(:articles),
                  subtitle:      "#{tt :page} #{@page_number}",
                  feed_url:      feed_path,
                  json_feed_url: feed_path(format: :json) %>
