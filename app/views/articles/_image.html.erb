<% if article.image.present? %>
  <div class="article-image">
    <%= link_to_if linked,
                   image_tag(article.image, class: "u-photo", alt: article.image_description),
                   article.path %>
  </div>
<% end %>
