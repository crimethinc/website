<% if tool.image_jpg.present? && tool.image_jpg.analyzed? %>
  <%= tool.image_jpg.metadata[:width] %>px
  x
  <%= tool.image_jpg.metadata[:height] %>px

<% elsif tool.respond_to?(:width) && tool.present? %>
  <%# TEMP transitional %>
  <%= tool.width %>
  x
  <%= tool.height %>

<% end %>
