<% if @collection %>
  <h1>
    <%= link_to current_resource_name.pluralize, [:admin, current_resource_name.tableize.to_sym] %>
  </h1>

  <h2>
    Add to the <%= link_to @collection.title, [:admin, @collection] %> Collection
  </h2>
<% else %>
  <%= render 'admin/page_header', thing: @article %>
<% end %>

<%= render "form", article: @article %>
