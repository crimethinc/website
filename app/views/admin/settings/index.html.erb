<%= render "admin/page_header", thing: @settings %>

<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Content</th>
    </tr>
  </thead>

  <tbody>
    <% @settings.each do |setting| %>
      <tr>
        <td>
          <%= link_to 'EDIT', [:edit, :admin, setting], class: 'btn btn-primary btn-sm' %>
        </td>
        <td><%= link_to setting.name,     [:admin, setting] %></td>
        <td><%= setting.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= paginate @settings, views_prefix: "admin", theme: "twitter-bootstrap-4" %>
