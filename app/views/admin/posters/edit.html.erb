<%= render "admin/page_header", thing: @poster %>

<% if @poster.english? %>
  <%= render 'admin/translate_this', article: @poster %>
<% end %>

<%= render "admin/posters/form", resource: @poster %>

<%= render "admin/danger_zone",
           thing: @poster,
           label: "poster",
           path: [:admin, @poster] %>
