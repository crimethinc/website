<!DOCTYPE html>
<%= tag.html dir:    html_dir,
             lang:   html_lang,
             id:     html_id,
             class:  html_class,
             prefix: html_prefix do %>
  <head>
    <%= render_themed "shared/head" %>
  </head>

  <body id="<%= body_id %>">
    <%= render_themed "shared/admin_header" %>
    <%= render_themed "shared/development_mode_banner" %>
    <%= render_themed "shared/header" %>

    <main>
      <%= yield %>
    </main>

    <%= render_themed "shared/footer" %>
    <%= render_themed "shared/javascripts" %>
  </body>
<% end %>
