<!DOCTYPE html>
<html dir="ltr" lang="<%= lang %>" prefix="og: http://ogp.me/ns#" id="<%= @slug %>">

<head>
  <%= render "shared/head" %>
</head>

<body id="<%= @slug %>">
  <%= render "shared/header" %>

  <main role="main">
    <%= yield %>
  </main>

  <%= render "shared/footer" %>
</body>
</html>
